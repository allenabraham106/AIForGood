# Gemini reflection-question API (3A backend) — sponsor stack
# Get a key from https://aistudio.google.com/apikey
# For local dev with Vercel: run "npx vercel dev" and set this in .env.local
GEMINI_API_KEY=your_gemini_api_key_here
